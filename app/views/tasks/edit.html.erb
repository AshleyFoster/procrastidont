<h1> Edit Task </h1>

<%= form_for @task do |f| %>
  <%= render "form", f: f, task: @task %>
  <%= f.submit "Update task" %>
<% end %><br>
<%= link_to "Delete Task", task_path(@task), method: :delete %>

